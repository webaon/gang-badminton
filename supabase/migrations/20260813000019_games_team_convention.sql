-- =============================================================================
-- WO-2.6 · 0019 — บันทึกข้อตกลงการแบ่งทีมลงในสคีมา (ADR-003)
-- =============================================================================
-- `games` มีช่องผู้เล่น 4 ช่องเรียงกันโดยไม่มีคอลัมน์ทีม ⇒ ความหมายอยู่ที่ **ลำดับ**
-- ซึ่งมองไม่เห็นจากตัวสคีมาเอง
--
-- 🔴 คนที่มาอ่านตารางนี้ทีหลังจะเดาเองว่า 1&2 vs 3&4 (ซึ่งถูก) แต่ไม่มีทางรู้ว่า
--    ลำดับถูกจัดมาแล้วให้ "แข็งสุด+อ่อนสุด" อยู่ฝั่งเดียวกัน ⇒ ถ้าเขียนโค้ดที่
--    เรียงใหม่เองจะทำลายสมดุลทีมโดยไม่รู้ตัว
--
-- migration นี้ไม่เปลี่ยนโครงสร้างใดๆ — เพิ่ม comment อย่างเดียว
-- แต่จำเป็น เพราะสคีมาคือที่แรกที่คนอ่านเวลาสงสัย
--
-- Rollback: comment on column ... is null; (ไม่มีผลต่อข้อมูล)
-- =============================================================================

comment on table public.games is
  'เกมต่อคอร์ท — [ADR-003] ลำดับผู้เล่นมีความหมาย: player1+player2 = ทีม A, player3+player4 = ทีม B. Matching Engine จัดให้แข็งสุดคู่กับอ่อนสุด (teamsOf() ใน domain/matching). shuttles_used เป็น numeric เพราะแบ่งลูกกันได้';

comment on column public.games.player1_registration_id is '[ADR-003] ทีม A คนที่ 1 — คู่กับ player2';
comment on column public.games.player2_registration_id is '[ADR-003] ทีม A คนที่ 2 — คู่กับ player1';
comment on column public.games.player3_registration_id is '[ADR-003] ทีม B คนที่ 1 — คู่กับ player4';
comment on column public.games.player4_registration_id is '[ADR-003] ทีม B คนที่ 2 — คู่กับ player3';
